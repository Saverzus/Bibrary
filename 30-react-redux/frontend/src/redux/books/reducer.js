import * as a from './actionTypes'

const initialState = []

const booksRecuder = (state = initialState, action) => {
    switch (action.type) {
        case a.ADD_BOOK:

            break;

        default:
            break;
    }
}